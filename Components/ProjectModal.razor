<MudDialog>
    <TitleContent>
        <MudText Typo="Typo.h5">
            @MudDialog.Title
        </MudText>
    </TitleContent>
    <DialogContent>
        <div class="row py-5">
            <div class="col-lg-6 col-12">
                <MudCarousel Class="mud-width-full" Style="height:250px;" ShowArrows="true" ShowBullets="true"
                             EnableSwipeGesture="@true" AutoCycle="@true" TData="object">
                    <BulletTemplate Context="selected">
                        <div Class="mud-button-root mud-icon-button mud-icon-button-color-inherit mud-ripple mud-ripple-icon">
                            <span class="mud-icon-button-label">
                                <MudIcon Icon="@(selected ? Icons.Outlined.RadioButtonChecked : Icons.Outlined.RadioButtonUnchecked)" Color="@Color.Dark"/>
                            </span>
                        </div>
                    </BulletTemplate>
                    <PreviousButtonTemplate>
                        <div Class="mud-button-root mud-icon-button mud-icon-button-color-inherit mud-ripple mud-ripple-icon">
                            <span class="mud-icon-button-label">
                                <MudIcon Class="mud-ripple mud-ripple-icon mud-icon-button-size-medium" Icon="@Icons.Outlined.KeyboardArrowLeft" Color="@Color.Dark"/>
                            </span>
                        </div>
                    </PreviousButtonTemplate>
                    <NextButtonTemplate>
                        <div Class="mud-button-root mud-icon-button mud-icon-button-color-inherit mud-ripple mud-ripple-icon">
                            <span class="mud-icon-button-label">
                                <MudIcon Class="mud-ripple mud-ripple-icon mud-icon-button-size-medium" Icon="@Icons.Outlined.KeyboardArrowRight" Color="@Color.Dark"/>
                            </span>
                        </div>
                    </NextButtonTemplate>
                    <ChildContent>
                        @foreach (var image in Project.Images)
                        {
                            <MudCarouselItem Transition="Transition.Slide" Color="@Color.Primary">
                                <div style="background-image: url(@image)"
                                     class="img-fluid project-image h-100">
                                </div>
                            </MudCarouselItem>
                        }
                    </ChildContent>
                </MudCarousel>
            </div>
            <div class="col-lg-6 col-12 pt-lg-0 pt-4">
                @Project.Description
            </div>
        </div>
    </DialogContent>
</MudDialog>